# Farcaster launch cast(s)

> Channels to crosspost: `/base`, `/dev`, `/ai`, `/builders`, `/founders`.

---

## Lead cast (post to home + /base)

introducing BaseKit — the agent skill kit for Base.

5 SKILL.md playbooks + 6 MCP tools.
one command in claude code, cursor, codex, windsurf.
free. MIT. ships today.

→ basekit.dev
→ github.com/basekit/basekit

[attach: 8s gif: install → "deploy a token on Base sepolia" → verified contract]

---

## Reply cast 1 (the philosophy)

we built it so an agent can install it without a human in the loop.

/llms.txt /agents.txt /skill.md /plugin.json — predictable paths, machine-readable.

if your agent finds basekit.dev, it can install the whole kit by following links alone.

---

## Reply cast 2 (call to onchain builders)

what should the next skill be?

candidates we're scoping:
- base-deploy-nft (ERC-721 / 1155, lazy mint, metadata pin)
- base-treasury (multi-sig spend plan + gnosis-safe txs)
- base-prediction-market (polymarket-style binary on Aerodrome)
- base-mev-resistant (private mempool, flashbots-on-base)

cast me your vote 🩵
