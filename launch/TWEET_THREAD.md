# Launch tweet thread — X / Twitter

> Time it for **Tuesday 9am PT** (post-coffee, pre-standup). Reply to thread with
> the demo gif after t+30 min for the algorithm.

---

**Tweet 1 (hook)**

introducing BaseKit.

ship onchain on Base from a single prompt — no copy-paste, no scaffolding, no "how do I init foundry again."

free, MIT, works in claude code / cursor / codex / windsurf.

🧵 ↓

[attach: 8-second gif of `claude plugin install basekit` → `deploy a token on base sepolia` → verified Basescan link]

basekit.dev

---

**Tweet 2 (the wedge)**

it's two things in one install:

→ 5 SKILL.md playbooks your agent loads on demand
→ 6 MCP tools your agent can call directly

skills give the agent the *playbook*. MCP gives it the *arms*.

---

**Tweet 3 (the five skills, in one screenshot)**

the 5 skills shipping today:

01 — base-deploy-token (ERC-20 from a prompt to verified on Basescan)
02 — base-analyze-wallet (activity, holdings, counterparties, risk)
03 — base-gas-optimize (audit Solidity for waste, prioritized diffs)
04 — base-airdrop (Merkle drops + sybil filter + claim contract)
05 — base-basescan-debug (paste a failed tx hash, get the root cause)

---

**Tweet 4 (the install line, for the demo crowd)**

one line, any environment.

claude plugin install basekit
cursor mcp add basekit
codex mcp add basekit npx -- -y @basekit/mcp
windsurf mcp add basekit

then just ask:
"deploy an ERC-20 on Base sepolia"

the right skill activates automatically.

---

**Tweet 5 (the agent-first angle — the contrarian play)**

we built BaseKit so an agent can install it without a human in the loop.

every manifest at a predictable path:
/llms.txt /agents.txt /skill.md /plugin.json /.well-known/mcp.json

if an LLM finds basekit.dev, it can install the whole kit by following links alone.

---

**Tweet 6 (open source, philosophical)**

what we believe:

→ one prompt → one outcome. no "what would you like next?"
→ show cost (gas + ETH at current base fee) before any tx
→ sepolia by default. mainnet only when named explicitly
→ never broadcast without confirmation
→ free + MIT. fork it, ship it.

---

**Tweet 7 (close — pricing tease)**

core is free forever.

pro ($19/mo) drops monthly with advanced playbooks — MEV-resistant deploys, multisig workflows, treasury automation. hosted indexing if you don't want to BYO alchemy key.

waitlist open: basekit.dev/#pricing

---

**Tweet 8 (CTA, the "do this now")**

install, try it, tell me what skill should come next.

github.com/basekit/basekit
basekit.dev

ship 🩵
